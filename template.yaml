base:
  - |+
    digraph G {
      compound=true
      newrank=true
      rankdir = LR

      labelloc="t"
      label="
        ${titulo}
        ${versao}

        ${produto}

  - type: loop
    dict: caracteristicas
    repeat: |
      ${fase}
  - |
    "
  - ['conteudo']
  - |
    }
conteudo:
  - |
    Oi_${mundo}
